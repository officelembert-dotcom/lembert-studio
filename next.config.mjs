/** @type {import('next').NextConfig} */
const nextConfig = {
  // Static export removed — Keystatic admin panel requires a Node.js server.
  // Vercel handles this automatically on the free tier.
  images: {
    unoptimized: true,
  },
}

export default nextConfig
